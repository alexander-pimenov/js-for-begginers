var firstName = "Jay";
var lastName = "Cutler";

if((firstName=="Jay") && (lastName=="Cutler")) {
  document.write("true");
} else {
  document.write("false");
}

// operators the same as for Java: && - end, || - or.
