// Math Objects:

document.write(Math.PI);

var number = prompt("Enter some number");
var answer = Math.sqrt(number);

alert("Square root of number " + number + " is " + answer);
// in total:
// Math is an object for math operations.
